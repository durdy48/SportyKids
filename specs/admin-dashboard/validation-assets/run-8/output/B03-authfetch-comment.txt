authFetch found: true
Explanation comment found: true

Comment lines near authFetch:
  L127: // authFetch passes the URL as-is (no internal base-URL prepending),
  L128: // so we must always provide the full URL via API_BASE here.

AuthFetch usage lines:
  L16: import { authFetch, API_BASE } from '@/lib/api';
  L127: // authFetch passes the URL as-is (no internal base-URL prepending),
  L130: authFetch(`${API_BASE}/admin/overview`),
  L131: authFetch(`${API_BASE}/admin/analytics/activity-chart`),
  L132: authFetch(`${API_BASE}/admin/jobs`),
