authFetch found: true
Explanation comment found: true

Comment lines near authFetch:
  L104: // authFetch passes the URL as-is (no internal base-URL prepending),
  L105: // so we must always provide the full URL via API_BASE here.

AuthFetch usage lines:
  L17: import { authFetch, API_BASE } from '@/lib/api';
  L104: // authFetch passes the URL as-is (no internal base-URL prepending),
  L107: authFetch(`${API_BASE}/admin/overview`),
  L108: authFetch(`${API_BASE}/admin/analytics/activity-chart`),
